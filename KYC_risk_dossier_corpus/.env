# --- Cloud SQL Connection (Private IP) ---
# If you are running locally and have network access to the private IP:
DB_HOST=""
# If you are deploying to Cloud Run and using its integration:
# DB_HOST="127.0.0.1"  <-- This is handled by --add-cloudsql-instances flag, not needed in env for Cloud Run

DB_NAME=""
DB_USER=""
DB_PASSWORD=""

# --- Google Cloud Storage ---
GCS_BUCKET_NAME=""


# --- Google Cloud Authentication ---
# Point this to the path of your service account JSON key file

GOOGLE_APPLICATION_CREDENTIALS=""
